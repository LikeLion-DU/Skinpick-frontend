<?xml version="1.0" encoding="utf-8"?>
<!--
  로컬 개발 서버는 HTTP다. Android 9+ 는 cleartext 를 기본 차단하므로
  이 파일이 없으면 첫 API 호출이 앱에서 막히고, 원인을 서버에서 찾게 된다.

  전체 허용(base-config cleartextTrafficPermitted="true")은 쓰지 않는다.
  배포 서버는 HTTPS이고, 열어두면 릴리즈 빌드까지 평문이 허용된다.

  실기기에서 개발 PC를 직접 호출해야 하면 그 PC의 LAN IP 를 한 줄 추가한다.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
